Optional combined question-set folder. Copy selected CSVs here if you want a single experiment over multiple curated groups.
Do not copy manifest or label-map CSVs here. Keep only files with a `question` column.
